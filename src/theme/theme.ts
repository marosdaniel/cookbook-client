import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      // main: green[400],
      main: '#088F8F',
      // main: '#097969',
      // main: '#40B5AD',
    },
  },
});

export default theme;
